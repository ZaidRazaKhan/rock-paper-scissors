public class Main {
    public static void main(String[] args) {
        /*
        only the logic for computation is implemented,
        The code can be easily extended such that it starts storing player information, and match result stats
         */
        CommandLineDriver commandLineDriver = new CommandLineDriver();
        commandLineDriver.startGame();
    }


}